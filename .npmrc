# The docs toolchain (nacara, gatsby 3) pins react 16/17, and Lit.React needs 18 or
# newer for createRoot. Nothing builds those docs: the workflow is manual-only, has
# never run, and asks for a node version the runners stopped offering. So the peer
# range loses, here rather than in whichever command someone types.
legacy-peer-deps=true
